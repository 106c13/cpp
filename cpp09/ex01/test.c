
sturct point {
	int x, y;
}


char *ptof(point *ptr) {
	char *buf;

	malloc....
	if (== NULL) {
		goto out;
	}
	snprintf(buf, sizeof(buf), "<ptr x: %d, ptr y: %d>", x, y);
	return buf
}

int main() {
	point p1 = {1, 2}, p2 = {3, 4}, p3;
	
	srand(getpid());
	p3.x = rand() % 1000;
	p3.y = rand();

	char *a = ptof();
	char *b = ptof();
	char *c = ptof();

	printf("%s %s %s\n",a, b, c,);
out: 
	if a == null , freea
	if a == null , freea
	if a == null , freea
}

432 45, 432 45, 432 45.
